# ── API Base URL ──
# Points to local FastAPI backend in development, Render backend URL in production
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# ── App Environment ──
NEXT_PUBLIC_APP_ENV=development
